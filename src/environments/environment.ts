// API configuration. Point apiBaseUrl at your Spring Boot backend.
// Use '' (empty) only if the API is served from the SAME origin as the app.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080', // <-- change to your backend host/port
};
