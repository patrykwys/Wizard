import {
  Check,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  Plus,
  Search,
  SquarePen,
} from 'lucide-angular';

// One place to register the icons used across the wizard, so each component
// just imports LucideAngularModule and references ICONS in its template.
export const ICONS = {
  Check,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  Plus,
  Search,
  SquarePen,
} as const;
